<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Geometric Matcher</title>
    <script src="https://cdn.jsdelivr.net/pyodide/v0.24.1/full/pyodide.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #6366f1;
            --secondary: #ec4899;
            --bg-dark: #0f172a;
            --glass: rgba(255, 255, 255, 0.05);
            --border: rgba(255, 255, 255, 0.1);
            --success: #10b981;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', sans-serif;
            background: radial-gradient(circle at top left, #1e293b, #0f172a);
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
            margin-top: 20px;
        }

        .header h1 {
            font-weight: 800;
            font-size: 2.5rem;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
        }

        .header p { color: #94a3b8; font-size: 1rem; }

        .main-card {
            background: var(--glass);
            backdrop-filter: blur(12px);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 30px;
            width: 100%;
            max-width: 1200px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .upload-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        .upload-zone {
            position: relative;
            border: 2px dashed var(--border);
            border-radius: 16px;
            height: 250px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            background: rgba(0,0,0,0.2);
            overflow: hidden;
        }

        .upload-zone:hover {
            border-color: var(--primary);
            background: rgba(99, 102, 241, 0.05);
        }

        /* --- CAMBIOS APLICADOS AQUÍ --- */
        
        .upload-zone.has-image {
            border-style: solid;
            border-color: var(--success);
        }

        /* Esta regla oculta el texto cuando hay imagen */
        .upload-zone.has-image .upload-content {
            display: none;
        }

        /* Esta regla hace la imagen totalmente visible */
        .upload-zone.has-image .preview-bg {
            opacity: 1;
        }

        /* ------------------------------ */

        .upload-zone input { display: none; }

        .upload-content {
            z-index: 2;
            text-align: center;
            pointer-events: none;
        }

        .icon { font-size: 3rem; margin-bottom: 10px; opacity: 0.7; }
        .label-text { color: #94a3b8; font-weight: 500; }

        .preview-bg {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            object-fit: contain;
            opacity: 0.6; /* Opacidad inicial para ver el texto detrás */
            z-index: 1;
            padding: 10px;
            transition: opacity 0.3s ease;
        }

        .actions {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .btn {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            padding: 16px 40px;
            border-radius: 12px;
            color: white;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px -5px rgba(236, 72, 153, 0.4);
        }

        .btn:disabled {
            background: #334155;
            cursor: not-allowed;
            opacity: 0.7;
        }

        .status-bar {
            text-align: center;
            margin-bottom: 20px;
            min-height: 24px;
            font-weight: 500;
            color: #e2e8f0;
        }

        .status-bar.loading { color: var(--primary); }
        .status-bar.success { color: var(--success); }
        .status-bar.error { color: #ef4444; }

        .canvas-container {
            background: #000;
            border-radius: 16px;
            overflow: hidden;
            border: 1px solid var(--border);
            box-shadow: inset 0 0 20px rgba(0,0,0,0.5);
            min-height: 100px;
            display: none;
        }

        canvas {
            display: block;
            width: 100%;
            height: auto;
        }

        .spinner {
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 50%;
            border-top-color: white;
            animation: spin 1s ease-in-out infinite;
            display: none;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
        
        @media (max-width: 768px) {
            .upload-grid { grid-template-columns: 1fr; }
            .header h1 { font-size: 2rem; }
        }
    </style>
</head>
<body>

    <div class="header">
        <h1>Geometric Matcher AI</h1>
        <p>Emparejamiento híbrido: Detecta formas (Círculos, Triángulos) + Detalles (ORB)</p>
    </div>

    <div class="main-card">
        
        <div class="upload-grid">
            <label class="upload-zone" id="zoneA">
                <img id="prevA" class="preview-bg" style="display:none;">
                <div class="upload-content">
                    <div class="label-text">Click para subir Imagen A</div>
                    <div style="font-size: 0.8rem; color: #64748b; margin-top: 5px;">(Referencia)</div>
                </div>
                <input type="file" id="imageA" accept="image/*">
            </label>

            <label class="upload-zone" id="zoneB">
                <img id="prevB" class="preview-bg" style="display:none;">
                <div class="upload-content">
                    <div class="label-text">Click para subir Imagen B</div>
                    <div style="font-size: 0.8rem; color: #64748b; margin-top: 5px;">(Búsqueda desordenada)</div>
                </div>
                <input type="file" id="imageB" accept="image/*">
            </label>
        </div>

        <div class="status-bar" id="status">
            <span id="statusText">Iniciando motor Python (Pyodide)...</span>
            <div class="spinner" id="spinner" style="display: inline-block;"></div>
        </div>

        <div class="actions">
            <button id="processBtn" class="btn" disabled>
                <span class="spinner" id="btnSpinner"></span>
                <span> Ejecutar Análisis</span>
            </button>
            <button id="downloadBtn" class="btn" style="background: #334155; display: none;">
                Descargar Resultado
            </button>
        </div>

        <div class="canvas-container" id="canvasContainer">
            <canvas id="resultCanvas"></canvas>
        </div>

    </div>

    <script>
        let pyodide = null;
        let imgDataA = null;
        let imgDataB = null;

        async function initSystem() {
            try {
                const statusText = document.getElementById('statusText');
                
                pyodide = await loadPyodide();
                statusText.textContent = "Cargando librerías OpenCV & NumPy...";
                
                await pyodide.loadPackage(['numpy', 'opencv-python']);
                
                statusText.textContent = "Sistema listo. Sube tus imágenes.";
                document.getElementById('status').classList.add('success');
                document.getElementById('spinner').style.display = 'none';
                checkReady();
            } catch (err) {
                document.getElementById('statusText').textContent = "Error al cargar Python. Recarga la página.";
                document.getElementById('status').classList.add('error');
            }
        }

        function handleFile(input, type) {
            const file = input.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = (e) => {
                const img = new Image();
                img.onload = () => {
                    const canvas = document.createElement('canvas');
                    canvas.width = img.width;
                    canvas.height = img.height;
                    const ctx = canvas.getContext('2d');
                    ctx.drawImage(img, 0, 0);
                    
                    const data = ctx.getImageData(0, 0, canvas.width, canvas.height);
                    
                    if (type === 'A') {
                        imgDataA = data;
                        const prev = document.getElementById('prevA');
                        prev.src = e.target.result;
                        prev.style.display = 'block';
                        // La clase 'has-image' dispara el CSS que oculta el texto
                        document.getElementById('zoneA').classList.add('has-image');
                    } else {
                        imgDataB = data;
                        const prev = document.getElementById('prevB');
                        prev.src = e.target.result;
                        prev.style.display = 'block';
                        // La clase 'has-image' dispara el CSS que oculta el texto
                        document.getElementById('zoneB').classList.add('has-image');
                    }
                    checkReady();
                };
                img.src = e.target.result;
            };
            reader.readAsDataURL(file);
        }

        function checkReady() {
            if (pyodide && imgDataA && imgDataB) {
                const btn = document.getElementById('processBtn');
                btn.disabled = false;
                btn.style.opacity = "1";
            }
        }

        document.getElementById('imageA').addEventListener('change', function() { handleFile(this, 'A'); });
        document.getElementById('imageB').addEventListener('change', function() { handleFile(this, 'B'); });

        document.getElementById('processBtn').addEventListener('click', async () => {
            const btn = document.getElementById('processBtn');
            const btnSpinner = document.getElementById('btnSpinner');
            const statusText = document.getElementById('statusText');
            const container = document.getElementById('canvasContainer');
            
            btn.disabled = true;
            btnSpinner.style.display = 'block';
            statusText.textContent = "⏳ Procesando geometría y descriptores...";
            document.getElementById('status').className = "status-bar loading";
            container.style.display = 'none';

            try {
                pyodide.globals.set('raw_A', pyodide.toPy(imgDataA.data));
                pyodide.globals.set('w_A', imgDataA.width);
                pyodide.globals.set('h_A', imgDataA.height);
                
                pyodide.globals.set('raw_B', pyodide.toPy(imgDataB.data));
                pyodide.globals.set('w_B', imgDataB.width);
                pyodide.globals.set('h_B', imgDataB.height);

                const pythonCode = `
import cv2
import numpy as np
import io

def process_img(data, w, h):
    arr = np.frombuffer(data, dtype=np.uint8).reshape((h, w, 4))
    gray = cv2.cvtColor(arr, cv2.COLOR_RGBA2GRAY)
    _, binary = cv2.threshold(gray, 127, 255, cv2.THRESH_BINARY)
    corners = [binary[0,0], binary[0,-1], binary[-1,0], binary[-1,-1]]
    if np.mean(corners) > 127:
        binary = cv2.bitwise_not(binary)
    return binary, cv2.cvtColor(gray, cv2.COLOR_GRAY2BGR)

def get_features(img_bin, orb):
    contours, _ = cv2.findContours(img_bin, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    objs = []
    for cnt in contours:
        area = cv2.contourArea(cnt)
        if area < 50: continue 
        moments = cv2.moments(cnt)
        if moments['m00'] == 0: continue
        hu = cv2.HuMoments(moments).flatten()
        hu = -1 * np.sign(hu) * np.log10(np.abs(hu) + 1e-10)
        cx = int(moments['m10']/moments['m00'])
        cy = int(moments['m01']/moments['m00'])
        x,y,w,h = cv2.boundingRect(cnt)
        mask = np.zeros(img_bin.shape, dtype=np.uint8)
        cv2.drawContours(mask, [cnt], -1, 255, -1)
        roi = img_bin[y:y+h, x:x+w]
        roi_mask = mask[y:y+h, x:x+w]
        kp, des = orb.detectAndCompute(roi, roi_mask)
        objs.append({ 'hu': hu, 'area': area, 'des': des, 'center': (cx, cy), 'bbox': (x,y,w,h) })
    return objs

imgA, viewA = process_img(raw_A, w_A, h_A)
imgB, viewB = process_img(raw_B, w_B, h_B)
orb = cv2.ORB_create(nfeatures=1000)
objsA = get_features(imgA, orb)
objsB = get_features(imgB, orb)
bf = cv2.BFMatcher(cv2.NORM_HAMMING, crossCheck=True)
matches_data = []

for i, a in enumerate(objsA):
    best_score = -1
    best_idx = -1
    for j, b in enumerate(objsB):
        dist_hu = np.linalg.norm(a['hu'] - b['hu'])
        shape_score = max(0, 100 - (dist_hu * 45))
        ratio_area = min(a['area'], b['area']) / max(a['area'], b['area'])
        area_score = ratio_area * 100
        orb_score = 0
        has_orb_match = False
        if a['des'] is not None and b['des'] is not None and len(a['des']) > 2 and len(b['des']) > 2:
            try:
                m = bf.match(a['des'], b['des'])
                if len(m) > 0:
                    top_m = sorted(m, key=lambda x:x.distance)[:5]
                    avg_dist = sum(x.distance for x in top_m)/len(top_m)
                    orb_score = max(0, 100 - avg_dist)
                    if orb_score > 40: has_orb_match = True
            except: pass
        if has_orb_match:
            final_score = (orb_score * 0.5) + (shape_score * 0.3) + (area_score * 0.2)
        else:
            final_score = (shape_score * 0.7) + (area_score * 0.3)
        if final_score > best_score:
            best_score = final_score
            best_idx = j
    if best_score > 60:
        matches_data.append({'ia': i, 'ib': best_idx, 'score': best_score})

max_h = max(h_A, h_B)
canvas = np.zeros((max_h, w_A + w_B, 3), dtype=np.uint8)
canvas[:h_A, :w_A] = viewA
canvas[:h_B, w_A:] = viewB
colors = [(0, 255, 255), (255, 0, 255), (0, 255, 0), (255, 255, 0), (255, 128, 0), (0, 128, 255)]

for idx, m in enumerate(matches_data):
    oa = objsA[m['ia']]
    ob = objsB[m['ib']]
    color = colors[idx % len(colors)]
    x,y,w,h = oa['bbox']
    cv2.rectangle(canvas, (x,y), (x+w,y+h), color, 2)
    x,y,w,h = ob['bbox']
    cv2.rectangle(canvas, (x+w_A,y), (x+w+w_A,y+h), color, 2)
    pt1 = oa['center']
    pt2 = (ob['center'][0] + w_A, ob['center'][1])
    cv2.line(canvas, pt1, pt2, color, 1)
    mid = (int((pt1[0]+pt2[0])/2), int((pt1[1]+pt2[1])/2))
    cv2.circle(canvas, mid, 12, (0,0,0), -1)
    cv2.putText(canvas, str(idx+1), (mid[0]-5, mid[1]+5), cv2.FONT_HERSHEY_SIMPLEX, 0.4, (255,255,255), 1)

is_success, buffer = cv2.imencode(".png", canvas)
io.BytesIO(buffer).getvalue()
                `;

                const pyResult = await pyodide.runPythonAsync(pythonCode);
                const blob = new Blob([pyResult.toJs()], { type: 'image/png' });
                const url = URL.createObjectURL(blob);

                const img = new Image();
                img.onload = () => {
                    const canvas = document.getElementById('resultCanvas');
                    container.style.display = 'block';
                    canvas.width = img.width;
                    canvas.height = img.height;
                    const ctx = canvas.getContext('2d');
                    ctx.drawImage(img, 0, 0);
                    
                    statusText.textContent = `Análisis completado exitosamente.`;
                    document.getElementById('status').className = "status-bar success";
                    btn.disabled = false;
                    btnSpinner.style.display = 'none';
                    document.getElementById('downloadBtn').style.display = 'inline-flex';
                    URL.revokeObjectURL(url);
                };
                img.src = url;

            } catch (error) {
                console.error(error);
                statusText.textContent = `Error: ${error.message}`;
                document.getElementById('status').className = "status-bar error";
                btn.disabled = false;
                btnSpinner.style.display = 'none';
            }
        });

        document.getElementById('downloadBtn').addEventListener('click', () => {
            const link = document.createElement('a');
            link.download = 'resultado_match.png';
            link.href = document.getElementById('resultCanvas').toDataURL();
            link.click();
        });

        initSystem();
    </script>
</body>
</html>
