import pymupdf
import re

def get_swatches(doc):
    swatches = {}
    for xref in range(1, doc.xref_length()):
        try:
            obj_str = doc.xref_object(xref, compressed=False)
        except:
            continue
        if "/Separation" in obj_str:
            match = re.search(r'/Separation\s+/([^\s/\]>]+)', obj_str)
            if match:
                name = match.group(1)
                values = re.findall(r'\b(0(?:\.\d+)?|1(?:\.0+)?|\.\d+)\b', obj_str)
                floats = [float(v) for v in values]
                swatches[name] = floats
                
                # Imprimir detalle para diagnóstico
                print(f"\n--- {name} ---")
                print(f"Valores ({len(floats)}): {floats}")
                print(f"Objeto raw:\n{obj_str[:500]}")  # primeros 500 chars
    return swatches

doc = pymupdf.open("tu_archivo.pdf")
swatches = get_swatches(doc)
